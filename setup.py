from setuptools import find_packages, setup

setup(
    name='pyvino',
    packages=find_packages(),
    version='0.1.0',
    description='openvino for python',
    author='hampen',
    license='Apache',
)
