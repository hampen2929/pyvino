from setuptools import find_packages, setup

setup(
    name='vinopy',
    packages=find_packages(),
    version='0.1.0',
    description='openvino for python',
    author='hampen',
    license='Apache',
)
